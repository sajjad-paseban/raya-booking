## درباره پروژه

پروژه Booking به‌عنوان یک نمونه‌کار آزمایشی برای شرکت فراز رایا طراحی و پیاده‌سازی شده است. هدف از این پروژه، ارزیابی توانایی و تسلط توسعه‌دهنده بر فریم‌ورک Laravel 12 و آشنایی با معماری مدرن و اصول توسعه‌ی نرم‌افزارهای تحت وب بوده است.

در این پروژه، یک سیستم رزرو (Booking System) طراحی شده که قابلیت مدیریت زمان‌بندی رزرو را فراهم می‌کند. ساختار پروژه با رعایت اصول MVC و RESTful API توسعه یافته است تا امکان توسعه و نگهداری آسان فراهم شود.

از مهم‌ترین ویژگی‌های پروژه می‌توان به موارد زیر اشاره کرد:

- استفاده از Laravel 12 برای پیاده‌سازی سمت سرور
- طراحی دیتابیس بهینه و استفاده از Eloquent ORM
- اعتبارسنجی داده‌ها و مدیریت خطاها به‌صورت ساخت‌یافته

این پروژه به‌عنوان یک نمونه‌ی عملی، نشان‌دهنده‌ی درک عمیق از مفاهیم توسعه در لاراول، طراحی پایگاه داده، مدیریت نشست‌ها و پیاده‌سازی منطق‌های تجاری در محیط‌های واقعی است.


## ⚙️ مراحل اجرای پروژه

برای اجرای پروژه مراحل زیر را به‌ترتیب انجام دهید:

**نصب پکیج‌های مورد نیاز لاراول**
```bash
    composer install
```

**ایجاد فایل تنظیمات محیط (.env)**
```bash
    cp .env.example .env
```
   سپس تنظیمات مربوط به پایگاه داده را ویرایش کنید:
   - نام دیتابیس را به دلخواه انتخاب کنید.
   - نوع اتصال (Connection) را مشخص کنید (برای مثال: `mysql`).

**تولید کلید امنیتی برنامه**
```bash
   php artisan key:generate
```

**اجرای مایگریشن‌ها (ایجاد جداول)**
```bash
   php artisan migrate
```
   در صورت درخواست برای ایجاد دیتابیس، گزینه‌ی تأیید را انتخاب کنید.

**درج داده‌های اولیه (Seeder)**
```bash
   php artisan db:seed
```
   این مرحله داده‌های پیش‌فرض مانند اطلاعات اتاق‌ها را در پایگاه داده وارد می‌کند.

**اجرای پروژه**
```bash
   php artisan serve
```
   سپس از طریق آدرس زیر به پروژه دسترسی خواهید داشت:
```
   http://localhost:8000
```

---

## ⏰ اجرای Jobها و زمان‌بندی‌ها

برای اجرای زمان‌بندی‌ها و Job ها در پس‌ زمینه از دستور زیر استفاده کنید:

```bash
    php artisan schedule:work
```


## تست پروژه

برای اجرای تست‌های واحد و اطمینان از عملکرد بخش‌های مختلف پروژه، دستور زیر را اجرا کنید:

```bash
    php artisan test
```


## تست API‌ها با Postman

برای تست و بررسی APIها، در پوشه‌ی `postman/` دو فایل قرار دارد:
- فایل کالکشن (Collection)
- فایل محیط (Environment)

برای استفاده:
1. نرم‌افزار **Postman** را باز کنید.
2. از منوی **Import** فایل‌های موجود در پوشه `postman/` را وارد کنید.
3. پس از اجرای سرور (`php artisan serve`) می‌توانید درخواست‌ها را تست کنید.

---

## 👤 توسعه‌دهنده
**طراح و پیاده‌ساز:** سجاد پاسبان رضوی خراسانی
**شرکت:** فراز رایا  

---